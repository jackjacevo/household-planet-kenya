import DeliveryLocations from '@/components/delivery/DeliveryLocations';

export default function DeliveryPage() {
  return <DeliveryLocations />;
}

export const metadata = {
  title: 'Delivery Locations - Household Planet Kenya',
  description: 'View all our delivery locations across Kenya with competitive pricing and fast delivery times.',
};